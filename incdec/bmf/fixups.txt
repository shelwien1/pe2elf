# fixups.txt — per-function literal substitutions applied to the donor body
# before extraction.  Tab-separated: function, find, replace, why.
#
# This is for the cases incdec.md §8 is about: Hex-Rays emits something its own
# type model implies but that g++ will not compile.  Every entry here is a
# *reinterpreting cast* — on i386 all of these are 4-byte-to-4-byte, so they
# change no code, only what the front end will accept.  Anything that would
# change behaviour belongs in override/ as a hand-written .inc, not here.
#
# `find` is matched literally against BMF.c, so you can grep for it, and
# extract.py fails loudly if it stops matching — a fixup cannot rot silently
# into a no-op when the donor is re-decompiled.
#
sub_412E60	v32[1] -= v52;	v32[1] -= (unsigned int)v52;	v52 is a counter (`v52 = (unsigned __int16 *)((char *)v52 + 1)`) that Hex-Rays typed as a pointer. `unsigned -= pointer` is not valid C either; the instruction is `sub [ecx+4], edx`.
sub_424D90	sub_4248D0(v103, v102, (int)Block);	sub_4248D0(v103, (_DWORD *)v102, (int)Block);	Hex-Rays types this argument `unsigned char **` at the call site and `_DWORD *` in sub_4248D0's own signature. Same pointer either way.
sub_424D90	sub_4248D0(v104, v103, (int)v102);	sub_4248D0(v104, (_DWORD *)v103, (int)v102);	As above.
sub_429DB0	strcpy(Str[0].m128i_i8, *argv);	strcpy(Str[0].m128i_i8, *(char **)argv);	The global at 0x00445954 is the CRT's `char **__argv` — `main` is declared `main(int argc, const char **argv, ...)` — but the data section types it `int argv;`, so `*argv` dereferences an int.
sub_41CAB0	v82 = v581;	v82 = (_BYTE *)v581;	Hex-Rays types v82 `_BYTE *` and v581 `_DWORD *`; the instruction is a register move.
sub_419430	sub_412E60(&v28[12 * v19 + 3 * v17])	sub_412E60((_DWORD *)&v28[12 * v19 + 3 * v17])	v28 is a `_QWORD[2]` local Hex-Rays also indexes far past its own bound; sub_412E60's parameter is `_DWORD *`. Same address.
sub_4273F0	sub_4248D0((unsigned __int8 **)v5, nullptr, 0);	sub_4248D0((_DWORD *)v5, nullptr, 0);	The cast at the call site disagrees with sub_4248D0's own `_DWORD *` parameter.
sub_40F450	buf = ::buf;	buf = (char *)::buf;	Local `char *buf` assigned the global at 0x00443380, which the same body indexes as `int[]`. Same address.
sub_417E80	ArgList_6 = ArgList_5;	ArgList_6 = (_DWORD *)ArgList_5;	`_DWORD *ArgList_6` and `char *ArgList_5`; the instruction is a register move.
sub_41CAB0	v502 = &v385[2 * n0x10_2 + 235020];	v502 = (_WORD *)&v385[2 * n0x10_2 + 235020];	`_WORD *v502` from a `_DWORD *` base. Hex-Rays scales the index by the base type either way, so the address is what it computed.
sub_417E80	ArgList_7 = ArgList_5;	ArgList_7 = (_DWORD *)ArgList_5;	As above, a second copy of the same register move.
sub_41CAB0	v488 = &v385[2 * n0x10_2 + 235020];	v488 = (_WORD *)&v385[2 * n0x10_2 + 235020];	Same as v502 above: a `_WORD *` taken from a `_DWORD *` base.
sub_41CAB0	v477 = &v385[2 * n0x10_2 + 235020];	v477 = (_WORD *)&v385[2 * n0x10_2 + 235020];	As above.
sub_41CAB0	v466 = &v385[2 * n0x10_2 + 235020];	v466 = (_WORD *)&v385[2 * n0x10_2 + 235020];	As above.
sub_424550	v39 = v38 - *(_DWORD *)(this + 8);	v39 = v38 - *(int *)(this + 8);	`__int64 v39` fed to `(BYTE4(v39) ^ v39) - BYTE4(v39)`, the sign-mask abs() of `cdq; xor eax,edx; sub eax,edx` at 0x004247FA. `int - _DWORD` is *unsigned*, so widening to __int64 zero-extends, BYTE4 is always 0 and the abs vanishes. Same 32 bits either way; only the conversion to 64 differs.
sub_40A8A0	v9 = *(double *)__libm_sse2_log().m128_u64 * 1.442695040888963;	v9 = __libm_sse2_log((double)*(int *)((char *)buf_2 + 4 * n2 + 0x342B8) / (double)*(_DWORD *)(dword_442E3C[n2] + 4)) * 1.442695040888963;	0x0040A926: the argument is in xmm0 and Hex-Rays recovered nothing, so it printed `__libm_sse2_log()`. Put back from the disassembly: `mov eax, dword_442E3C[ebx*4]; mov edx, [eax+4]` is the denominator, `cvtsi2sd xmm0, [eax+ebx*4+342B8h]` with eax reloaded from buf_2 is the numerator.
sub_40A8A0	v15 = *(double *)__libm_sse2_log().m128_u64 * 1.442695040888963;	v15 = __libm_sse2_log((double)*(int *)((char *)buf_2 + 0x342E4) / (double)*(_DWORD *)(dword_442E68 + 4 * n24)) * 1.442695040888963;	0x0040A9AB, same problem. `mov eax, dword_442E68; mov edx, [eax+esi*4]` is the denominator with esi = n24; `cvtsi2sd xmm0, [eax+342E4h]` off buf_2 is the numerator.
sub_40A8A0	v32 = *(double *)__libm_sse2_log().m128_u64 * 1.442695040888963;	v32 = __libm_sse2_log((double)*(int *)((char *)buf_2 + 0x342E8) / (double)*(_DWORD *)(dword_442E6C + 4 * *((unsigned __int8 *)buf_2 + n6 + 213056))) * 1.442695040888963;	0x0040ACEF, same problem. The denominator is the guard's own expression; the numerator is `[ecx+342E8h]` with ecx = buf_2.
sub_40A8A0	v6 = *(double *)__libm_sse2_log().m128_u64 * 1.442695040888963;	v6 = __libm_sse2_log((double)*(int *)((char *)buf_2 + 0x342D0) / (double)*(_DWORD *)(dword_442E54[0] + 4 * ((2 * n24) | (n6 - 5)))) * 1.442695040888963;	0x0040A8CB, same problem. The denominator is the guard's own expression; the numerator is `[eax+342D0h]` with eax = buf_2.
sub_40A8A0	v25 = *(double *)__libm_sse2_log().m128_u64;	v25 = __libm_sse2_log((double)*(this + v23 + 49664) / (double)v39);	0x0040ABF4, same problem. `cvtsi2sd xmm0, [esp+var_64]` / `cvtsi2sd xmm3, [esp+var_50]`, which the two `mov`s above load from `[ecx+edi*4+30800h]` (0x30800/4 = 49664) and `[eax+30900h]` (v39).
sub_40A8A0	v29 = *(this + v23 + 49664) <= 2 * v28 ? 1.0 : *(double *)__libm_sse2_log().m128_u64 * 1.442695040888963;	v29 = *(this + v23 + 49664) <= 2 * v28 ? 1.0 : __libm_sse2_log((double)*(this + v23 + 49664) / (double)v28) * 1.442695040888963;	0x0040AC8F, same problem. `cvtsi2sd xmm0, edx` where edx is the guard's own left-hand side, over `cvtsi2sd xmm3, eax` where eax is v28.
sub_402EF0	int __stdcall __noreturn sub_402EF0(char *FileName, int a2)	int __userpurge __noreturn sub_402EF0@<eax>(_DWORD *v2@<ecx>, char *FileName, int a2)	IDA typed this __stdcall and Hex-Rays left `_DWORD *v2; // ecx` undefined — it flags it itself: "402EF4: variable 'v2' is possibly undefined". The instruction at 0x00402EF4 is `mov edi, ecx`, so there is a third argument in ecx on top of the two on the stack, and 0x00402F8C-0x00402F92 is `mov eax, edi; retn 8` — callee-pops with the result in eax. That is __userpurge, and without the correction the moved body reads whatever g++ left in ecx.
sub_402EF0	  _DWORD *v2; // ecx		Now the first parameter, see above.
sub_436E10	int __usercall sub_436E10@<eax>(__m128d a1@<xmm1>)	__m128d __usercall sub_436E10@<xmm0>(__m128d a0@<xmm0>, __m128d a1@<xmm1>)	IDA recovered one of the two register arguments and the wrong return register. 0x00436E1F-0x00436E29 is `andpd xmm0, xmm1; andnpd xmm1, xmm5; orpd xmm0, xmm1` — a select whose other input is xmm0, which the signature does not mention — and the caller at 0x0041177F uses xmm0 afterwards (`mulpd xmm1, xmm0`), never eax, which the tail call clobbers anyway. The body is rewritten in override/.
sub_411700	        sub_436E10((__m128d)_mm_xor_si128((__m128i)xmmword_4397A0, (__m128i)v18));	        __m128d v19 = sub_436E10((__m128d)v17, (__m128d)_mm_xor_si128((__m128i)xmmword_4397A0, (__m128i)v18));	sub_436E10's xmm0 argument and its result both went missing (see the fixup on its own signature). At 0x0041177F xmm0 holds `cvtdq2pd xmm2` — v17 — and the two instructions after the call are `movaps xmm1, [esp+var_30]; mulpd xmm1, xmm0`, so the result is multiplied by v17.
sub_411700	        v15 = _mm_add_pd(v15, (__m128d)_mm_andnot_ps(v18, (__m128)_mm_mul_pd((__m128d)v17, (__m128d)v17)));	        v15 = _mm_add_pd(v15, (__m128d)_mm_andnot_ps(v18, (__m128)_mm_mul_pd((__m128d)v17, v19)));	Having lost sub_436E10's result, Hex-Rays squared v17 instead: `mulpd xmm1, xmm0` at 0x0041178E multiplies v17 by the log, not by itself.
sub_411700	          v14 = v14 + (double)v11 * *(double *)__svml_log2().m128_u64;	          v14 = v14 + (double)v11 * __svml_log2((__m128i)_mm_set1_pd((double)v11)).m128d_f64[0];	0x00411805-0x00411809: `cvtsi2sd xmm0, edx; unpcklpd xmm0, xmm0; call ___svml_log2`, so the argument is v11 broadcast to both lanes, and `mulsd xmm1, xmm0` afterwards takes lane 0.
sub_411700	      v8 = v8 * *(double *)__libm_sse2_log().m128_u64;	      v8 = v8 * __libm_sse2_log(v8);	0x00411850: `movaps xmm0, xmm3` with xmm3 = v8, so the argument is v8 itself.
sub_4015C0	if ( operator new(8u) )	if ( void *__nb = operator new(8u) )	The pointer `operator new(8u)` returns is sub_402EF0's ecx argument (`mov ecx, eax` at 0x00401724 and at every other call site), and Hex-Rays discarded it along with the argument itself — see the fixup on sub_402EF0's signature. Name it so the calls below can pass it.
sub_4028B0	if ( operator new(8u) )	if ( void *__nb = operator new(8u) )	The pointer `operator new(8u)` returns is sub_402EF0's ecx argument (`mov ecx, eax` at 0x00401724 and at every other call site), and Hex-Rays discarded it along with the argument itself — see the fixup on sub_402EF0's signature. Name it so the calls below can pass it.
sub_4015C0	sub_402EF0(FileName_1, 0);	v56 = sub_402EF0((_DWORD *)__nb, FileName_1, 0);	sub_402EF0 takes a third argument in ecx that IDA's __stdcall signature does not mention (§8.2.4.1); it is the block allocated by the `operator new(8u)` in the enclosing `if`. It also returns that same pointer in eax (`mov eax, edi; retn 8`), which is what the caller keeps — Hex-Rays dropped the result too.
sub_4015C0	sub_402EF0(FileName_2, 0);	v24 = sub_402EF0((_DWORD *)__nb, FileName_2, 0);	sub_402EF0 takes a third argument in ecx that IDA's __stdcall signature does not mention (§8.2.4.1); it is the block allocated by the `operator new(8u)` in the enclosing `if`. It also returns that same pointer in eax (`mov eax, edi; retn 8`), which is what the caller keeps — Hex-Rays dropped the result too.
sub_4015C0	sub_402EF0(FileName_3, 0);	p_i_13 = sub_402EF0((_DWORD *)__nb, FileName_3, 0);	sub_402EF0 takes a third argument in ecx that IDA's __stdcall signature does not mention (§8.2.4.1); it is the block allocated by the `operator new(8u)` in the enclosing `if`. It also returns that same pointer in eax (`mov eax, edi; retn 8`), which is what the caller keeps — Hex-Rays dropped the result too.
sub_4015C0	sub_402EF0(FileName_4, 0);	v14 = sub_402EF0((_DWORD *)__nb, FileName_4, 0);	sub_402EF0 takes a third argument in ecx that IDA's __stdcall signature does not mention (§8.2.4.1); it is the block allocated by the `operator new(8u)` in the enclosing `if`. It also returns that same pointer in eax (`mov eax, edi; retn 8`), which is what the caller keeps — Hex-Rays dropped the result too.
sub_4015C0	sub_402EF0(FileNamea, 1);	Block = (void *)sub_402EF0((_DWORD *)__nb, FileNamea, 1);	sub_402EF0 takes a third argument in ecx that IDA's __stdcall signature does not mention (§8.2.4.1); it is the block allocated by the `operator new(8u)` in the enclosing `if`. It also returns that same pointer in eax (`mov eax, edi; retn 8`), which is what the caller keeps — Hex-Rays dropped the result too.
sub_4015C0	sub_402EF0(FileName_6, 0);	p_i_6 = sub_402EF0((_DWORD *)__nb, FileName_6, 0);	sub_402EF0 takes a third argument in ecx that IDA's __stdcall signature does not mention (§8.2.4.1); it is the block allocated by the `operator new(8u)` in the enclosing `if`. It also returns that same pointer in eax (`mov eax, edi; retn 8`), which is what the caller keeps — Hex-Rays dropped the result too.
sub_4028B0	sub_402EF0(FileNamea, 0);	v18 = (FILE **)sub_402EF0((_DWORD *)__nb, FileNamea, 0);	sub_402EF0 takes a third argument in ecx that IDA's __stdcall signature does not mention (§8.2.4.1); it is the block allocated by the `operator new(8u)` in the enclosing `if`. It also returns that same pointer in eax, which is what the caller keeps.
sub_4015C0	      p_i_4 = sub_403820((int)Block, a1, a2, 0, (int *)&dwLowDateTime);	      p_i_4 = (_DWORD *)sub_403820((int)Block, a1, a2, 0, (int *)&dwLowDateTime);	sub_403820 is declared returning `char *` and Hex-Rays assigns it to a `_DWORD *`. A return type cannot be relaxed the way a parameter can (§6.3), so the cast goes at the call site. Same pointer.
sub_4015C0	      dwLowDateTime = nullptr;	      dwLowDateTime = 0;	The global at 0x00445A4C is `void *dwLowDateTime` in the declaration section and `int` in symbols.txt, which is what wins; `= nullptr` does not convert. Same store either way.
sub_4015C0	  dwLowDateTime = nullptr;	  dwLowDateTime = 0;	As above, the second one.
sub_4015C0	        v56 = 0;	        else v56 = 0;	The zeroing is the *else* of the `if ( operator new(8u) )` above — 0x00401FDC is `xor esi, esi` on the not-taken branch, and the taken branch is `mov esi, eax` after the call.  Hex-Rays, having dropped both the argument and the result of sub_402EF0, flattened the two branches into an unconditional store.
sub_4015C0	        v24 = 0;	        else v24 = 0;	The zeroing is the *else* of the `if ( operator new(8u) )` above — 0x00401FDC is `xor esi, esi` on the not-taken branch, and the taken branch is `mov esi, eax` after the call.  Hex-Rays, having dropped both the argument and the result of sub_402EF0, flattened the two branches into an unconditional store.
sub_4015C0	        p_i_13 = 0;	        else p_i_13 = 0;	The zeroing is the *else* of the `if ( operator new(8u) )` above — 0x00401FDC is `xor esi, esi` on the not-taken branch, and the taken branch is `mov esi, eax` after the call.  Hex-Rays, having dropped both the argument and the result of sub_402EF0, flattened the two branches into an unconditional store.
sub_4015C0	        v14 = 0;	        else v14 = 0;	The zeroing is the *else* of the `if ( operator new(8u) )` above — 0x00401FDC is `xor esi, esi` on the not-taken branch, and the taken branch is `mov esi, eax` after the call.  Hex-Rays, having dropped both the argument and the result of sub_402EF0, flattened the two branches into an unconditional store.
sub_4015C0	  p_i_6 = 0;	  else p_i_6 = 0;	The zeroing is the *else* of the `if ( operator new(8u) )` above — 0x00401FDC is `xor esi, esi` on the not-taken branch, and the taken branch is `mov esi, eax` after the call.  Hex-Rays, having dropped both the argument and the result of sub_402EF0, flattened the two branches into an unconditional store.
sub_4028B0	  v18 = nullptr;	  else v18 = nullptr;	The zeroing is the *else* of the `if ( operator new(8u) )` above — 0x00401FDC is `xor esi, esi` on the not-taken branch, and the taken branch is `mov esi, eax` after the call.  Hex-Rays, having dropped both the argument and the result of sub_402EF0, flattened the two branches into an unconditional store.
sub_4015C0	    printf("File %16s,\r", FileNamea);	    else Block = nullptr; printf("File %16s,\r", FileNamea);	The zeroing is the *else* of the `if ( operator new(8u) )` above — 0x00401FDC is `xor esi, esi` on the not-taken branch, and the taken branch is `mov esi, eax` after the call.  Hex-Rays, having dropped both the argument and the result of sub_402EF0, flattened the two branches into an unconditional store.
sub_4015C0	    Block = nullptr;	    ;	The zeroing is the *else* of the `if ( operator new(8u) )` above — 0x00401FDC is `xor esi, esi` on the not-taken branch, and the taken branch is `mov esi, eax` after the call.  Hex-Rays, having dropped both the argument and the result of sub_402EF0, flattened the two branches into an unconditional store.
sub_4015C0	v56 = v83;	;	Each compress branch spills the block register to a stack slot before reusing it as a scratch pointer in the palette scan (`mov [esp+724h+var_710], ebp`), and restores it after.  Hex-Rays had already discarded what that register held — sub_402EF0's result, see its signature fixup — so it wrote the *spill* as a constant 0 and the restore then handed null to sub_402FE0.  In the decompilation the scratch is a separate variable and the block variable is not touched between the two, so the restore is a no-op: dropping it is both correct and the same in every branch, which the spill is not (two of them share a line).
sub_4015C0	v24 = *(_DWORD *)FileName_1;	;	Each compress branch spills the block register to a stack slot before reusing it as a scratch pointer in the palette scan (`mov [esp+724h+var_710], ebp`), and restores it after.  Hex-Rays had already discarded what that register held — sub_402EF0's result, see its signature fixup — so it wrote the *spill* as a constant 0 and the restore then handed null to sub_402FE0.  In the decompilation the scratch is a separate variable and the block variable is not touched between the two, so the restore is a no-op: dropping it is both correct and the same in every branch, which the spill is not (two of them share a line).
sub_4015C0	p_i_13 = (int)p_i_5;	;	Each compress branch spills the block register to a stack slot before reusing it as a scratch pointer in the palette scan (`mov [esp+724h+var_710], ebp`), and restores it after.  Hex-Rays had already discarded what that register held — sub_402EF0's result, see its signature fixup — so it wrote the *spill* as a constant 0 and the restore then handed null to sub_402FE0.  In the decompilation the scratch is a separate variable and the block variable is not touched between the two, so the restore is a no-op: dropping it is both correct and the same in every branch, which the spill is not (two of them share a line).
sub_4015C0	v14 = *(_DWORD *)FileName_1;	;	Each compress branch spills the block register to a stack slot before reusing it as a scratch pointer in the palette scan (`mov [esp+724h+var_710], ebp`), and restores it after.  Hex-Rays had already discarded what that register held — sub_402EF0's result, see its signature fixup — so it wrote the *spill* as a constant 0 and the restore then handed null to sub_402FE0.  In the decompilation the scratch is a separate variable and the block variable is not touched between the two, so the restore is a no-op: dropping it is both correct and the same in every branch, which the spill is not (two of them share a line).
sub_4015C0	p_i_6 = (int)p_i_5;	;	Each compress branch spills the block register to a stack slot before reusing it as a scratch pointer in the palette scan (`mov [esp+724h+var_710], ebp`), and restores it after.  Hex-Rays had already discarded what that register held — sub_402EF0's result, see its signature fixup — so it wrote the *spill* as a constant 0 and the restore then handed null to sub_402FE0.  In the decompilation the scratch is a separate variable and the block variable is not touched between the two, so the restore is a no-op: dropping it is both correct and the same in every branch, which the spill is not (two of them share a line).
sub_42BBD0	void __usercall sub_42BBD0(char a1@<fpstat>, int a2, int a3, int a4, int a5)	void __cdecl sub_42BBD0(int a2, int a3, int a4, int a5)	`@<fpstat>` is the x87 status word, not an argument — the same kind of pseudo-argument as the two USERCALL_STACK_ONLY entries.  Its only use is as the second argument of sub_4349F0, which is one of those entries and has that slot dropped anyway.  IDA's own stack layout agrees the real signature is four dwords (arg_0..arg_C), and the one place that takes this function's address casts it to `void (__cdecl *)(int, int, int, int)`.  Matches the forward declaration too, which is a comment.
sub_429840	Buffer_2 = a1 + 8;	Buffer_2 = (_BYTE *)(a1 + 8);	Hex-Rays typed Buffer_2 as _BYTE * and a1 as unsigned __int16 *, then assigned one to the other without a cast; C++ refuses. The arithmetic it meant is the donor's, `lea eax, [ecx+10h]` at 0x0042997E, which is a1 + 8 elements = +16 bytes.
sub_429840	Buffer_5 = a1 + 8;	Buffer_5 = (void *)(a1 + 8);	Same assignment one line later, into a `void *`.
main	if ( strrchr(ArgList, 92) )	if ( bmf_path_sep(ArgList) )	The donor separates directory from file name on '\\' only. bmf_path_sep takes the last '/' or '\\', so the same body works under the shim and standalone on POSIX.
main	strrchr(Str, 92)[1] = 0;	bmf_path_sep(Str)[1] = 0;	Same, one line on.
exit_402E40	void __stdcall __noreturn exit_402E40(int Code, char ArgList)	void __cdecl __noreturn exit_402E40(int Code, ...)	IDA typed the message printer __stdcall(int, char); the donor is cdecl variadic. `lea eax, [esp+8]` at 0x00402E44 is a va_list, and the function never cleans the stack because it never returns. The body is override/exit_402E40.inc; this row is what makes sites.txt record the convention the entry point actually has.
main	exit_402E40(2, (char)ArgList);	exit_402E40(2, ArgList);	Message 2 takes one `%s`; the donor pushes the pointer. Hex-Rays truncated it to a char to fit the signature it had invented.
main	exit_402E40(1, (char)argv[argc_1]);	exit_402E40(1, argv[argc_1]);	Message 1 takes one `%s`; the donor pushes the pointer. Hex-Rays truncated it to a char to fit the signature it had invented.
sub_4015C0	exit_402E40(6, (char)FileNamea);	exit_402E40(6, FileNamea);	Message 6 takes one `%s`; the donor pushes the pointer. Hex-Rays truncated it to a char to fit the signature it had invented.
sub_4015C0	exit_402E40(3, (char)FileNamea);	exit_402E40(3, FileNamea);	Message 3 takes one `%s`; the donor pushes the pointer. Hex-Rays truncated it to a char to fit the signature it had invented.
sub_4015C0	exit_402E40(5, (char)FileName_1);	exit_402E40(5, FileName_1);	Message 5 takes one `%s`; the donor pushes the pointer. Hex-Rays truncated it to a char to fit the signature it had invented.
sub_4015C0	exit_402E40(5, (char)FileName_2);	exit_402E40(5, FileName_2);	Message 5 takes one `%s`; the donor pushes the pointer. Hex-Rays truncated it to a char to fit the signature it had invented.
sub_4015C0	exit_402E40(5, (char)FileName_3);	exit_402E40(5, FileName_3);	Message 5 takes one `%s`; the donor pushes the pointer. Hex-Rays truncated it to a char to fit the signature it had invented.
sub_4015C0	exit_402E40(5, (char)FileName_4);	exit_402E40(5, FileName_4);	Message 5 takes one `%s`; the donor pushes the pointer. Hex-Rays truncated it to a char to fit the signature it had invented.
sub_4015C0	exit_402E40(5, (char)FileName_5);	exit_402E40(5, FileName_5);	Message 5 takes one `%s`; the donor pushes the pointer. Hex-Rays truncated it to a char to fit the signature it had invented.
sub_4015C0	exit_402E40(5, (char)FileName_6);	exit_402E40(5, FileName_6);	Message 5 takes one `%s`; the donor pushes the pointer. Hex-Rays truncated it to a char to fit the signature it had invented.
sub_4022C0	exit_402E40(5, (char)lpFileName_1);	exit_402E40(5, lpFileName_1);	Message 5 takes one `%s`; the donor pushes the pointer. Hex-Rays truncated it to a char to fit the signature it had invented.
sub_4022C0	exit_402E40(5, (char)lpFileName_2);	exit_402E40(5, lpFileName_2);	Message 5 takes one `%s`; the donor pushes the pointer. Hex-Rays truncated it to a char to fit the signature it had invented.
sub_4022C0	exit_402E40(5, (char)lpFileName);	exit_402E40(5, lpFileName);	Message 5 takes one `%s`; the donor pushes the pointer. Hex-Rays truncated it to a char to fit the signature it had invented.
sub_4028B0	exit_402E40(3, (char)FileName);	exit_402E40(3, FileName);	Message 3 takes one `%s`; the donor pushes the pointer. Hex-Rays truncated it to a char to fit the signature it had invented.
sub_4028B0	exit_402E40(5, (char)FileNamea);	exit_402E40(5, FileNamea);	Message 5 takes one `%s`; the donor pushes the pointer. Hex-Rays truncated it to a char to fit the signature it had invented.
sub_402EF0	exit_402E40(6, (char)FileName);	exit_402E40(6, FileName);	Message 6 takes one `%s`; the donor pushes the pointer. Hex-Rays truncated it to a char to fit the signature it had invented.
sub_402EF0	exit_402E40(3, (char)FileName);	exit_402E40(3, FileName);	Message 3 takes one `%s`; the donor pushes the pointer. Hex-Rays truncated it to a char to fit the signature it had invented.
sub_4015C0	exit_402E40(4, Format);	exit_402E40(4);	Message 4 is "Read error!", with no conversions, and the donor pushes nothing but the code (0x00401BDE and four more).
sub_4028B0	exit_402E40(4, Format);	exit_402E40(4);	Same, at 0x00402BBF.
sub_4149C0	exit_402E40(4, ArgList);	exit_402E40(4);	Same, at 0x00414A31.
sub_4022C0	exit_402E40(8, FileTime.dwLowDateTime);	exit_402E40(8);	Message 8 is "User break!", no conversions; 0x00402547 pushes only the code.
sub_402BD0	exit_402E40(2, v25 - 1);	exit_402E40(2, (const char *)(v25 - 1));	Message 2 is "Unknown option: %s"; the donor pushes `eax` after `dec eax` (0x00402D68), which is a char pointer. Hex-Rays typed the variable int, so the call needs the cast back.
sub_402BD0	exit_402E40(2, result - 1);	exit_402E40(2, (const char *)(result - 1));	Same, at 0x00402DCF.
main	exit_402E40(0, (char)off_44104C[dword_44108C]);	exit_402E40(0, (const char *)off_44104C[dword_44108C], n7_0, (const char *)off_44104C[dword_441090], n7_1, n2_4, (const char *)off_44104C[dword_441098 == 0], (const char *)*(&bmp_ + n2_5), (const char *)off_44104C[dword_442BA0], Destination_ ? &Destination_ : "none", (const char *)off_44104C[dword_442BAC], ElementCount, dword_441058, dword_44105C, n4_6, n0xFFFF, (const char *)off_44104C[dword_442BA8]);	The usage screen: sixteen conversions against sixteen pushes at 0x00401539..0x004015AD, of which Hex-Rays kept one, truncated to a char. Reading the pushes bottom-up gives the order here. `ebx` at the -N push is main's argc_1, which is 1 unless dword_441098 is non-zero, hence `== 0`; `eax` at the -O push is &Destination_ when the buffer is non-empty and "none" when it is not (0x0040151C-0x00401523).
sub_414F60	v4[559820] = 0x606060606060606LL;	*(_QWORD *)((char *)v4 + BMF_BLOB(0x00445660)) = 0x606060606060606LL;	An absolute data address hidden in a scaled index: v4 is a _QWORD * holding a small offset, and 559820 * 8 is 0x00445660. rebase_data_literals only sees literals that are addresses in themselves, so this one is spelled out.
sub_414F60	v4[559821] = 0x606060606060606LL;	*(_QWORD *)((char *)v4 + BMF_BLOB(0x00445668)) = 0x606060606060606LL;	An absolute data address hidden in a scaled index: v4 is a _QWORD * holding a small offset, and 559821 * 8 is 0x00445668. rebase_data_literals only sees literals that are addresses in themselves, so this one is spelled out.
sub_414F60	v4[559822] = 0x606060606060606LL;	*(_QWORD *)((char *)v4 + BMF_BLOB(0x00445670)) = 0x606060606060606LL;	An absolute data address hidden in a scaled index: v4 is a _QWORD * holding a small offset, and 559822 * 8 is 0x00445670. rebase_data_literals only sees literals that are addresses in themselves, so this one is spelled out.
sub_414F60	v4[559823] = 0x606060606060606LL;	*(_QWORD *)((char *)v4 + BMF_BLOB(0x00445678)) = 0x606060606060606LL;	An absolute data address hidden in a scaled index: v4 is a _QWORD * holding a small offset, and 559823 * 8 is 0x00445678. rebase_data_literals only sees literals that are addresses in themselves, so this one is spelled out.
sub_4149C0	v9[559820] = 0x606060606060606LL;	*(_QWORD *)((char *)v9 + BMF_BLOB(0x00445660)) = 0x606060606060606LL;	An absolute data address hidden in a scaled index: v9 is a _QWORD * holding a small offset, and 559820 * 8 is 0x00445660. rebase_data_literals only sees literals that are addresses in themselves, so this one is spelled out.
sub_4149C0	v9[559821] = 0x606060606060606LL;	*(_QWORD *)((char *)v9 + BMF_BLOB(0x00445668)) = 0x606060606060606LL;	An absolute data address hidden in a scaled index: v9 is a _QWORD * holding a small offset, and 559821 * 8 is 0x00445668. rebase_data_literals only sees literals that are addresses in themselves, so this one is spelled out.
sub_4149C0	v9[559822] = 0x606060606060606LL;	*(_QWORD *)((char *)v9 + BMF_BLOB(0x00445670)) = 0x606060606060606LL;	An absolute data address hidden in a scaled index: v9 is a _QWORD * holding a small offset, and 559822 * 8 is 0x00445670. rebase_data_literals only sees literals that are addresses in themselves, so this one is spelled out.
sub_4149C0	v9[559823] = 0x606060606060606LL;	*(_QWORD *)((char *)v9 + BMF_BLOB(0x00445678)) = 0x606060606060606LL;	An absolute data address hidden in a scaled index: v9 is a _QWORD * holding a small offset, and 559823 * 8 is 0x00445678. rebase_data_literals only sees literals that are addresses in themselves, so this one is spelled out.
