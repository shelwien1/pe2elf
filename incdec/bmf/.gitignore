# Run artifacts: everything below is produced by build.sh / test.sh / the
# debugging helpers and is reproducible from what is tracked.
dbg/
dbg[0-9]/
ct_ref/
ct_ours/
*.so
*.elf
*.o
dummy32.cpp
calltrace.out
trace.out
run/
tr/

# standalone/: blob.inc is 65 KB of BMF.exe's data segment carved out by
# mkdata.py, and build.sh regenerates it if it is missing.
standalone/bmf
standalone/bmf_standalone.cpp
standalone/blob.inc
standalone/run/
standalone/p/
standalone/w/
